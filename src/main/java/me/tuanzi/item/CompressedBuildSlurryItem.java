package me.tuanzi.item;

import net.minecraft.world.item.Item;

public class CompressedBuildSlurryItem extends Item {
    public CompressedBuildSlurryItem(Properties properties) {
        super(properties.stacksTo(64));
    }
}
